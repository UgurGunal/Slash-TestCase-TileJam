namespace TileMatch.Core
{
    /// <summary>Identity of a tile icon. Extend the enum or replace with ScriptableObject IDs later via adapters.</summary>
    public enum TileKind
    {
        A = 0,
        B = 1,
        C = 2,
        D = 3,
        E = 4,
        F = 5,
    }
}
